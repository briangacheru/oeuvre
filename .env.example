# Oeuvre / iTasker configuration template.
# Copy this file to .env and fill in the real values. Do not commit .env.

# Application — keep false in production so errors are logged, not shown to users
APP_DEBUG=false
# Public base URL of the writer app (no trailing slash). Admin area is APP_URL/sudo.
APP_URL=https://your-domain.example
# Login lockout: after this many wrong passwords in a row, the account is
# locked for LOGIN_LOCKOUT_HOURS. Applies to both writer and admin logins.
LOGIN_MAX_ATTEMPTS=5
LOGIN_LOCKOUT_HOURS=1

# Secret key used to encrypt task ids embedded in URLs (view-task?task_id=...).
# 64 hex chars (32 bytes). Generate with: php -r "echo bin2hex(random_bytes(32));"
# Changing this invalidates all previously issued task links.
TASK_ID_KEY=

# Database
DB_HOST=localhost
DB_USER=your_db_user
DB_PASS=your_db_password
DB_NAME=tasker

# Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=your_smtp_password
MAIL_FROM_ADDRESS=you@example.com
ADMIN_EMAIL=admin@example.com

# DigitalOcean Spaces
SPACES_REGION=sfo3
SPACES_ENDPOINT=https://sfo3.digitaloceanspaces.com
SPACES_BUCKET=your_bucket
SPACES_KEY=your_spaces_key
SPACES_SECRET=your_spaces_secret
# Optional CDN endpoint, e.g. https://your_bucket.sfo3.cdn.digitaloceanspaces.com
# SPACES_CDN_ENDPOINT=
